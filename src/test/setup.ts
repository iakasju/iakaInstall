// Setup minimal du harnais front. iakaInstall n'a qu'une seule charte (AR-I3,
// studio-clair) et pas d'i18n (FR seul, MVP) : rien a initialiser ici. Les
// assertions DOM des tests utilisent l'API standard (pas de jest-dom, pour ne
// pas alourdir la stack M-R7 d'une dependance non repliquee chez les soeurs).
export {};
