# LOCAL a chaque depot, hors fixtures/convergence.sha256 (motif : convergence.sha256:27-29,
# meme forme que fixtures/vitrine-locale.json). Sa donnee est PROPRE A CE PRODUIT — voir
# CONVERGENCE-RELEASE-YML-ALIGNEMENT (successeur AR-V4, CLAUDE.md backlog) : aligner avec le
# Cockpit installerait des paquets dont ce produit n'a pas besoin (aucune dependance
# audio/whisper ici).
#
# Convention Tauri Linux commune (webkit2gtk, indicateur, rsvg, patchelf, gtk3). Ce depot
# n'a NI cpal NI whisper-rs (src-tauri/Cargo.toml, mesure : grep -i "cpal\|whisper" ne
# retourne rien) : pas de paquet supplementaire.
libwebkit2gtk-4.1-dev
libappindicator3-dev
librsvg2-dev
patchelf
libgtk-3-dev
