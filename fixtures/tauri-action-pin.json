{
  "_lire": "L41 / D-4 — LE REFERENT EPINGLE DE `tauri-apps/tauri-action`. Ce fichier est la SOURCE du cliquet : il porte le SHA contre lequel le comportement de l'action a ete LU, et le jeu d'entrees que cette version DECLARE reellement. Toute mutation de test se fait ICI, jamais dans le workflow.",
  "_pourquoi": "Les workflows epinglaient `@v0` — un tag DEPLACABLE. Ils posaient `uploadUpdaterJson: false`, entree que la version reellement executee ne declare PAS : GitHub Actions ignore en SILENCE une entree inconnue. Le volet G de L40 etait donc inoperant sur ce qui s'execute. Le pin fige le referent ; le cliquet force a re-lire quand on le bouge.",
  "action": "tauri-apps/tauri-action",
  "sha": "84b9d35b5fc46c1e45415bdb6144030364f7ebc5",
  "version": "action-v0.6.2",
  "tagsPointantSurCeSha": ["action-v0.6.2", "v0.6", "v0"],
  "actionYmlSha256": "351738c494a482a2e583a0ecc93a0130e42c902fd91d6b938a8099fc3c5e879a",
  "luLe": "2026-08-29",
  "luComment": "git ls-remote refs/tags/v0^{} -> 84b9d35b ; curl raw.githubusercontent.com/<action>/84b9d35b/action.yml ; git fetch --depth 1 <sha> pour la source.",
  "entreesDeclarees": [
    "releaseId",
    "tagName",
    "releaseName",
    "releaseBody",
    "releaseDraft",
    "prerelease",
    "releaseCommitish",
    "projectPath",
    "distPath",
    "iconPath",
    "appName",
    "appVersion",
    "includeDebug",
    "includeRelease",
    "includeUpdaterJson",
    "updaterJsonPreferNsis",
    "updaterJsonKeepUniversal",
    "tauriScript",
    "args",
    "retryAttempts",
    "bundleIdentifier",
    "owner",
    "repo",
    "githubBaseUrl",
    "isGitea",
    "assetNamePattern",
    "uploadPlainBinary",
    "generateReleaseNotes"
  ],
  "entreesAbsentesVerifiees": ["uploadUpdaterJson", "uploadUpdaterSignatures"],
  "constatsLusDansLaSourceDuSha": {
    "includeUpdaterJson": "defaut 'true'. Consommee en `src/index.ts:30` puis `src/index.ts:223` : elle garde le SEUL appel a `uploadVersionJSON` — c'est-a-dire la pose du `latest.json` concurrent. La mettre a `false` supprime ce manifeste, et rien d'autre.",
    "sig": "Les `.sig` restent televerses. `uploadReleaseAssets(...artifacts...)` (`src/index.ts:212`) est appele SANS condition des qu'un `releaseId` existe, HORS du `if (includeUpdaterJson)`. Les `.sig` sont des artefacts a part entiere (`src/utils.ts:29-46`, poses par `src/build.ts:132` et suivants). Aucune entree ne gouverne leur televersement a ce SHA — il n'y a donc rien a laisser a un defaut.",
    "updaterJsonPreferNsis": "declaree, sans defaut explicite (donc chaine vide -> false). Non posee par nos workflows : sans objet des lors que `includeUpdaterJson` vaut `false`."
  },
  "_siLeShaChange": "NE PAS lever la garde en recopiant le nouveau SHA. Re-lire `action.yml` AU NOUVEAU SHA (les trois entrees `updater`), re-lire `src/index.ts` pour verifier que le televersement des `.sig` reste hors du garde `includeUpdaterJson`, PUIS mettre a jour ce fichier.",
  "notarisationConstateeLe2026-09-05": {
    "instruction": "specs/instructions/amorcage-c3-vitrine-trois-freres.md § 0.3, § 3 AR-V5",
    "sourceLue": "PAS ce SHA de tauri-action (action.yml n'y declare AUCUNE entree Apple, confirme : `curl raw.githubusercontent.com/tauri-apps/tauri-action/84b9d35b5fc46c1e45415bdb6144030364f7ebc5/action.yml` ne contient ni 'apple' ni 'sign' ni 'notari', 87 lignes). La notarisation est pilotee PAR LE BUNDLER RUST (crate tauri-bundler), pas par cette action : lu au tag `@tauri-apps/cli-v2.11.4` (celui que resout `package-lock.json` de ce depot, `@tauri-apps/cli` 2.11.4) sur `github.com/tauri-apps/tauri`.",
    "constat": "`crates/tauri-bundler/src/bundle/macos/app.rs:117-118` n'appelle `sign::keychain(identity)` qu'a l'interieur du bloc de signature ; `crates/tauri-bundler/src/bundle/macos/sign.rs:19-44` (`keychain()`) entre dans la branche 'Some' (donc DECLENCHE la signature puis `notarize_auth()`, app.rs:135) DES QUE `APPLE_CERTIFICATE` ET `APPLE_CERTIFICATE_PASSWORD` sont TOUTES DEUX `Some` via `env::var_os` — CE QUI EST VRAI MEME SI LEUR VALEUR EST UNE CHAINE VIDE, tant que la variable EXISTE dans l'environnement du process. Or GitHub Actions pose une variable `env:` qui reference un secret ABSENT comme une CHAINE VIDE mais PRESENTE (documente, pratique courante des workflows de la communaute). `Keychain::with_certificate` (`crates/tauri-macos-sign/src/keychain.rs:55-63`) decoderait alors un p12 VIDE (base64 vide -> fichier vide) et `security import` echouerait sur ce fichier invalide -> LE BUILD MACOS CASSE. C'est exactement le mode de defaillance de l'issue publique `tauri-apps/tauri-action#291` citee au cadrage (§ 0.4).",
    "verdictAR-V5": "(b) — declaration seule, AUCUN cablage `env:` APPLE_* sur l'etape `tauri-action` de `release.yml`. L'etape de declaration ajoutee juste avant `tauri-action` (job `build`) lit la presence des secrets via l'EXPRESSION GitHub Actions `${{ secrets.X }}` (substitution TEXTUELLE avant l'execution du shell, qui n'ecrit AUCUNE variable d'environnement dans le process enfant) — donc SANS le risque mesure ci-dessus. Motif ecrit aussi en commentaire dans `release.yml`, au-dessus de cette etape."
  }
}
