# Notes de version — v0.1.2

> Préparé par ⚒️ Gimli (branche `chore/ressource-cli-0.41.0`, 2026-09-06), à l'usage
> d'Aragorn pour la commande de tag. **Aucun tag n'a été posé par cet agent** — le tag
> déclenche `release.yml` (matrice 4 plateformes + publication), geste réservé au décideur.

## Contexte du bump

`main` embarquait la ressource CLI `@naonedge/iakaframe` en `0.40.0` depuis la v0.1.1. Le
dépôt sœur `iakaframe` a publié `v0.41.0` : les étapes 3/4 de la chaîne d'installation
(`iakaframe install`) — poser IakaCockpit et iakaFrameGUI — couvrent désormais **Linux**
(AppImage) et **Windows** (`.exe` NSIS `currentUser`), en plus de macOS. Le cadrage AR-P5(a)
de `specs/instructions/pilotage-reel-facade-contrat-machine.md` impose de remonter cette
ressource épinglée à chaque release de la façade : c'est l'objet exclusif de ce lot.

## Texte du tag annoté proposé

À jouer par le décideur (ou Aragorn sur son ordre), **après merge de
`chore/ressource-cli-0.41.0` dans `main`**, sur le commit qui porte
`package.json`/`tauri.conf.json`/`Cargo.toml` = `0.1.2` :

```bash
git tag -a v0.1.2 -F docs/releases/v0.1.2.tagmsg
```

## Ce qui a changé dans ce lot

- **Ressource CLI embarquée** : `fixtures/cli-embarque.json` remonté à `0.41.0` (asset
  `naonedge-iakaframe-0.41.0.tgz`, 697 967 octets, sha256
  `d8799b7d6ac32cb7d336def415588c1f739d78d8cce56336c42253615b2594f7`, re-mesuré par
  téléchargement direct). `npm run embarquer` vérifié AVANT extraction (CA-P13), 552 entrées
  extraites (111 sous `install.mjs`+`kits/`, conforme aux faits mesurés).
- **Vocabulaire fermé inchangé** (AR-W8) : `npm run vocabulaire` régénère
  `src/events/vocabulaire.ts` — seuls le commentaire de provenance et `VERSION_RESSOURCE`
  bougent, aucun nouveau `detail` ni nouvel état entre 0.40.0 et 0.41.0.
- **L'indice de couverture pré-flux (`src/coverage.ts`) mis à jour** : il affirmait encore
  « seule macOS est couverte » alors que `cleManifestePlateforme` (côté CLI 0.41.0) couvre
  désormais aussi Linux x64 et Windows x64. `etapes34Couvertes` prend l'architecture en
  compte ; le message de refus de `App.tsx` est reformulé. **L'installeur unifié devient
  complet sur les trois OS** pour ce que l'indice pré-flux annonce — la vérité au moment d'un
  flux réel reste, comme avant, celle de `etape-terminee.etat` (M-C6), jamais simulée.
- **Façade bumpée `0.1.1` → `0.1.2`** (`package.json`, `package-lock.json`,
  `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml`, `src-tauri/Cargo.lock`). `README.md`
  régénéré par `npm run vitrine -- --write` (3 zones : binaires/sécurité/sources).
- **Correctif incident** : la note de sécurité (`fixtures/vitrine-locale.json`,
  `macos-notarisation`) citait en dur `iakaInstall_0.1.1_aarch64.dmg` entre backticks — la
  garde anti-dérive de la vitrine (CA-10, `scripts/lib/vitrine.mjs`) lit tout nom d'artefact
  entre backticks comme une promesse du README ; au bump, ce nom figé sur une version passée
  devenait une fausse promesse non dérivable de la table courante. Reformulé sans répéter de
  nom de fichier versionné, la mesure historique (v0.1.1, 2026-09-05) reste dite intacte.
- **Fixture `fixtures/flux-apercu.ndjson` ré-enregistrée** contre la ressource 0.41.0 (rejeu
  réel `--dry-run --events` en bac à sable, `--target-claude`/`--apps-dir`/`--backup-dir` sous
  scratchpad, aucune écriture réelle constatée). 74 lignes, `fin`/`ok:true`, comme avant.
  Diff champ par champ (hors horodatage) contre l'ancienne fixture 0.40.0 : **seuls les
  numéros de version changent** — le flux macOS (étapes 3/4, `darwin-arm64`) est
  structurellement identique, aucune annonce Linux/Windows n'apparaît sur cette machine
  (attendu : les étapes 3/4 restent conditionnées à la plateforme réellement détectée).

## Preuve mesurée

- `npm run typecheck` → `0`.
- `npm run lint` → `0`.
- `npm run test` → `0`, **129 passed (129)** (avant : 127 — +2 tests, aucun supprimé).
- `npm run build` → `0`.
- `cargo test` (`src-tauri/`) → `0`, 22 passed.
- `cargo fmt --check` → `0`.
- `cargo clippy --all-targets -- -D warnings` → `0`.
- `npm run tauri build -- --target aarch64-apple-darwin`, lancé depuis un arbre **sans**
  ressource CLI préalable (`src-tauri/resources/cli` supprimé avant le build) → `0` ;
  `beforeBuildCommand` (`npm run embarquer && npm run build`) l'a reproduite automatiquement
  en 0.41.0. Vérifié dans le `.app` produit :
  `Contents/Resources/cli/package.json` → `"version": "0.41.0"`,
  `Contents/Resources/cli/_bundled/VERSION` → `v0.41.0`.
- `npm run vitrine:check` → `0` (README aligné sur v0.1.2).
- `npm run vitrine:en-ligne` → `1`, **attendu, HORS gate** : 2 écarts (E-2, E-3) tant que le
  tag `v0.1.2` n'est pas publié sur GitHub — même mécanisme qu'au bump précédent (v0.1.1).

## Ce que ce lot NE fait PAS / reste gate humain

- Ne pose aucun tag, ne publie rien, ne déclenche aucun workflow.
- **L'UAC sur un compte Windows non-administrateur** (installeur `.exe` NSIS `currentUser`) —
  non prouvée par ce lot, gate humain déclaré côté `iakaframe` (v0.41.0).
- **SmartScreen** au lancement de l'installeur Windows (aucun certificat de signature de code
  posé, cf. `fixtures/vitrine-locale.json` → `windows-signature`) — recette réelle requise.
- **Gatekeeper / notarisation macOS** (signature AD HOC, cf. `macos-notarisation`) — recette
  réelle requise, procédure Sequoia documentée dans le README.
- **La recette réelle sur les trois OS** (Windows, Linux, macOS Intel) de l'installeur
  unifié — hors périmètre de ce lot, qui ne fait que remonter la ressource CLI et republier
  la façade ; le lancement effectif de l'AppImage sur une distribution Linux réelle et le
  déroulé complet de l'installeur `.exe` sur un poste Windows réel restent `NON-MESURÉ` ici.

## Successeurs nommés (hors périmètre de ce lot, déjà déclarés au backlog)

Voir `CLAUDE.md` § Backlog : `ETAPES-3-4-WINDOWS-LINUX` (successeur transposé une fois la
recette réelle disponible), `UPDATER-DE-LA-FACADE`, `CONVERGENCE-TROIS-FRERES`,
`MARQUE-IAKAINSTALL`, `INSTALL-I18N`.

## Écart connu, attendu et non corrigé

`npm run vitrine:en-ligne` rendra un écart tant que `v0.1.2` n'existe pas réellement sur
GitHub — voulu et attendu, même mécanisme qu'au bump `v0.1.1`. L'écart se referme au moment
du tag + publication, pas avant.
